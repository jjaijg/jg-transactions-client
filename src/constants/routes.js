export const routes = {
  HOME: "/",
  TRANSACTIONS: "/transactions",
  CATEGORIES: "/categories",
};

export const authRoutes = {
  LOGIN: "/login",
  REGISTER: "/register",
};

export const protectedRoutes = {};
